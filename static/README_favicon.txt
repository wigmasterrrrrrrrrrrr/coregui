Place the provided favicon.ico in the following locations so it is served correctly:

- static/favicon.ico  (for development/static site)
- build/favicon.ico   (for the pre-built site in `build/`)

I updated templates and built HTML files to reference `/favicon.ico` or `favicon.ico`. If you want, I can add the attached binary `favicon.ico` file to the repo now — say the word and I'll commit it to `static/` and `build/`.